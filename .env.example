# Copy to .env for local-only values.
# Never commit real secrets.
EXAMPLE_SETTING=local-development

# Optional placeholders for local-only integrations.
# Keep real values in .env or your secret manager, never in Git.
PASSIVE_DNS_API_KEY=
SHODAN_API_KEY=
CENSYS_API_ID=
CENSYS_API_SECRET=
MCP_SERVER_LOG_LEVEL=info
